[project]
name = "cli-council"
version = "0.1.0"
description = "Multi-LLM collaboration system with three-stage deliberation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "python-telegram-bot>=21.0",
]
